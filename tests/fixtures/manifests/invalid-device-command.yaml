device:
  id: invalid-device-command
  profile: linux-wsl
capabilities:
  opencode: {required: true, config_profile: personal-development}
  providers: [opencode-cloud]
  mcp_servers: [github]
credentials:
  - id: command-secret
    reference: op://holdfast-lan/; rm -rf /
    inject_as: LITELLM_API_KEY
